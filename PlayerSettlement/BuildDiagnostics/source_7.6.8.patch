--- a/PlayerSettlement/BuildDiagnostics/CultureVisualHelper.cs
+++ b/PlayerSettlement/BuildDiagnostics/CultureVisualHelper.cs
@@ -111,7 +111,7 @@
                 if (source == null)
                     continue;
 
-                GameEntity copy = GameEntity.CopyFrom(scene, source, true, false);
+                GameEntity copy = GameEntity.CopyFromPrefab(source);
                 if (copy == null)
                     continue;
 
--- a/PlayerSettlement/BuildDiagnostics/PlayerSettlementBehaviour.cs
+++ b/PlayerSettlement/BuildDiagnostics/PlayerSettlementBehaviour.cs
@@ -3162,7 +3162,8 @@
                     {
                         var castleSettlement = CreateCastle(settlementName, culture, out PlayerSettlementItem castleItem);
 
-                        castleSettlement.Town.OwnerClan = Hero.MainHero.Clan;
+                        castleSettlement.Town.OwnerClan = null;
+                        castleSettlement.Town.OwnerClan = Clan.PlayerClan;
 
                         castleSettlement.SetName(new TextObject(settlementName));
 
@@ -4119,7 +4120,8 @@
                     {
                         Settlement townSettlement = CreateTown(settlementName, culture, out PlayerSettlementItem townItem);
 
-                        townSettlement.Town.OwnerClan = Hero.MainHero.Clan;
+                        townSettlement.Town.OwnerClan = null;
+                        townSettlement.Town.OwnerClan = Clan.PlayerClan;
 
                         townSettlement.SetName(new TextObject(settlementName));
 
--- a/PlayerSettlement/BuildDiagnostics/VillageRaidFix_7.6.7/PlayerSettlementVillageRaidFix.cs
+++ b/PlayerSettlement/BuildDiagnostics/VillageRaidFix_7.6.8/PlayerSettlementVillageRaidFix.cs
@@ -80,11 +80,21 @@
                     }
                     else
                     {
-                        if (bound.IsFortification && bound.OwnerClan == null &&
-                            IsPlayerSettlement(bound) && bound.Town != null && Clan.PlayerClan != null)
+                        if (bound.IsFortification)
                         {
-                            bound.Town.OwnerClan = Clan.PlayerClan;
-                            repairedReferences++;
+                            if (bound.OwnerClan == null)
+                            {
+                                bound.Town.OwnerClan = Clan.PlayerClan;
+                            }
+                            else if (bound.OwnerClan == Clan.PlayerClan)
+                            {
+                                // The owner loaded from XML can already equal PlayerClan before
+                                // Bannerlord's clan/kingdom fief caches have been populated. Force
+                                // the official ownership lifecycle to remove any stale entry and
+                                // register the fortification exactly once in the native caches.
+                                bound.Town.OwnerClan = null;
+                                bound.Town.OwnerClan = Clan.PlayerClan;
+                            }
                         }
 
                         if (bound.OwnerClan == null || bound.MapFaction == null)
--- a/PlayerSettlement/BuildDiagnostics/VillageRaidFix_7.6.7/PlayerSettlementVillageRaidFix.csproj
+++ b/PlayerSettlement/BuildDiagnostics/VillageRaidFix_7.6.8/PlayerSettlementVillageRaidFix.csproj
@@ -5,7 +5,7 @@
     <Nullable>disable</Nullable>
     <AssemblyName>PlayerSettlementVillageRaidFix</AssemblyName>
     <RootNamespace>PlayerSettlementVillageRaidFix</RootNamespace>
-    <Version>7.6.7</Version>
+    <Version>7.6.8</Version>
     <Platforms>x64</Platforms>
     <Deterministic>true</Deterministic>
   </PropertyGroup>
