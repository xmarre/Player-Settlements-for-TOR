PLAYER SETTLEMENT 7.6.11 - RUNTIME TEST PLAN

1. Delete Modules/PlayerSettlement and Modules/PlayerSettlement_TOR completely.
2. Install both folders from this archive and fully restart Bannerlord.
3. Load the same save that crashed in SettlementVisual.SetSettlementLevelVisibility.
4. Remain on the campaign map for at least one in-game hour; zoom, rotate, and move across the map.
5. Inspect every existing custom town, castle, and village, including banners and map icons.
6. Save to a new slot, exit to desktop, restart, and reload that new save.
7. Test a custom village raid from start through interruption or completion.
8. Verify hostile AI can select, approach, begin, and maintain a siege against a custom fortification.
9. Check wall/settlement visual state before, during, and after a siege.

If a protected-memory crash remains, attach the full crash report and state whether the
crash occurred immediately on map load or only after a particular custom settlement
entered view.
